import { Button } from "./ui/button";

export default function DownloadCV() {
  return (
    <Button className="body-2  rounded-[12px] bg-gray-900 px-4 py-1 text-gray-50 transition-all duration-300 hover:bg-primary-500 hover:text-gray-900">
      Download CV
    </Button>
  );
}
